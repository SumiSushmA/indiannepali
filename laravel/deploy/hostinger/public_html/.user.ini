; Hostinger PHP settings for Laravel 12 (PHP 8.2+)
; Adjust in hPanel → Advanced → PHP Configuration if .user.ini is ignored.

upload_max_filesize = 64M
post_max_size = 64M
memory_limit = 256M
max_execution_time = 120
max_input_vars = 3000
